from setuptools import setup

setup(name='TS-Platform',
      version='0.0.1',
      install_requires=['numpy', 'tinynet', 'cma', 'scipy', 'matplotlib', 'pandas', 'scikit-learn', 'pyDOE', 'pflacco',
                        'ray', 'deap']
)
